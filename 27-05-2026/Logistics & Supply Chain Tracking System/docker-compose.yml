services:
  api:
    build:
      context: ./LogisticsAPI
    ports:
      - "5183:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Docker
      ASPNETCORE_URLS: http://+:8080

  ui:
    build:
      context: ./logisticsui
    ports:
      - "3000:80"
    depends_on:
      - api
